import React from "react";


const UserContext = React.createContext()
// UserCOntext is a variable value provider its a wrapper 
export default UserContext

{/* <UserContext>
    <Login/>
    <Card>
        <Data/>
    </Card>
</UserContext> 

here UserContext is a provider(a wrapper) giving access of the states of variable to all the conponents inside  */}


// NOw we will make a provider jo ki ek jsx hoga as it is sort of a component
